function hlBuildSuggest(products,budget){const groups=['CPU','Mainboard','RAM','SSD','Nguồn','VGA','Màn hình'];let used=0;return groups.map(g=>{const p=(products||[]).filter(x=>String(x.category||'').toLowerCase().includes(g.toLowerCase())).sort((a,b)=>(a.salePrice||a.price||0)-(b.salePrice||b.price||0))[0]; if(p) used+=Number(p.salePrice||p.price||0); return p||{name:'Chưa chọn '+g,category:g,price:0};}).filter(Boolean)}
